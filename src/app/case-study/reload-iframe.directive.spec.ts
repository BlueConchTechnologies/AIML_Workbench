import { ReloadIframeDirective } from './reload-iframe.directive';

describe('ReloadIframeDirective', () => {
  it('should create an instance', () => {
    const directive = new ReloadIframeDirective();
    expect(directive).toBeTruthy();
  });
});
